payload = {
  '{{ service_name }}': [
    {
      'version': '{{ version }}',
      'description': 'pre-interview technical test',
      'lastcommitsha': '{{ git_commit_sha }}',
      'log_level': '{{ log_level }}'
    }
  ]
}